'use strict';

describe('movies.home module', function() {

    beforeEach(module('movies.home'));

    describe('MovieSearchCtrl controller', function(){

        it('should ....', inject(function($controller) {
            //spec body
            var view1Ctrl = $controller('MovieSearchCtrl');
            expect(view1Ctrl).toBeDefined();
        }));
    });
});
